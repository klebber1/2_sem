#include <stdio.h>

int main() {
    int num;
    for (int i = 5; i <=5< ; ++i) {
        do {
            printf("digite um numero maior que zero: (inteiro)\n");
            scanf("%d",&num);
        } while (num<=0);
        int soma =0;
        for (int j = 1; j <=(num/2) ; ++j) {
            if (num % j -= 0) soma += j;
        }
        printf("somatoria dos divisores ");
    }
    return 0;
}
