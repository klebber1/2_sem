#include <stdio.h>
#include <stdlib.h>
#include <time.h>
int main() {
    srand(time(NULL));
    int matriz [4][4];
    int aleatorio = rand()%10;
    for (int i = 0; i < 4; ++i) {
        for (int j = 0; j < 4; ++j) {

        }

    }

    printf("aleatorio: %d\n",aleatorio);
    return 0;
}
